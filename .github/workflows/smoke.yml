name: Reusable smoke

on:
  workflow_call:

permissions:
  contents: read

jobs:
  repo-check:
    name: repo-check
    runs-on: ubuntu-24.04
    steps:
      - name: Confirm shared workflow access
        run: echo shared-workflow-ok
